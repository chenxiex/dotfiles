{{- if (and (eq .chezmoi.hostname "anlorsp-main") (eq .chezmoi.os "linux")) -}}
#!/usr/bin/bash
mkdir -p ~/.config/ibus/rime
cd ~/.config/ibus/rime
ln -sf -t . ../rime_source/oh-my-rime/* 
ln -sf -t . ../rime_source/rime_custom/*
ln -sf -t . ../rime_source/wanxiang/*
{{- end }}
